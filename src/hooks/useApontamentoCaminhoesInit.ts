
import { useEffect } from 'react';

export const useApontamentoCaminhoesInit = () => {
  useEffect(() => {
    // Storage buckets are now created via SQL migration
    // No initialization needed
  }, []);
};
